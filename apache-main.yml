---
  - name: Check for apache and install if mia
    hosts: local
    become: True
#    ask-become-pass: True
    tasks:
      - name: Install httpd if mia
        apt:
          name: apache2
          state: latest

